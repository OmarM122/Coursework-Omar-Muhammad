//Want to create 3 variables
//For each variable, we need too use a different operator
//For the first code 10, we can use 5 + 5
//Create a dialog box
//Add script tag 
//Use string con cantenation or template literal to display the code using out variables

const addition = 5 + 5
const multiplication = 5 * 8
const subtraction = 40 - 1
alert(`${addition} - ${multiplication} - ${subtraction} `)
alert(addition + dash + multiplication + dash + subtraction) 
